{
  "name": "cad-monorepo",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "start": "npm run resource:start",
    "web:dev": "npm run resource:web:dev",
    "web:build": "npm run resource:web:build",
    "resource:start": "npm --prefix resource run start",
    "resource:web:dev": "npm --prefix resource run web:dev",
    "resource:web:build": "npm --prefix resource run web:build",
    "desktop:dev": "npm --prefix desktop run dev",
    "desktop:build": "npm --prefix desktop run build"
  }
}
